#!/bin/bash -x
if (( $RANDOM %2 -eq 1))
empRatePer=10
R=$(( $RANDOM ))
responce=$(( $R % 2 ))
then
echo "Employee present"
elif [ $response -eq 0]
echo "Employye parttime"
else
echo "employee is absent"
fi

